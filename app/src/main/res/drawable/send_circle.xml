<?xml version="1.0" encoding="utf-8"?>
<!-- The primary action of the conversation screen: a filled circle, so it
     reads as a button without a label competing with the message field. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">
    <solid android:color="@color/colorPrimary" />
</shape>
